[
  {
    "title": "Two Sum",
    "slug": "two-sum",
    "description": "Given an array of integers `nums` and an integer `target`, return indices of the two numbers such that they add up to `target`.\n\n**Input Format:**\n- First line: Integer `n` (size of array)\n- Second line: `n` space-separated integers (`nums`)\n- Third line: Integer `target`\n\n**Output Format:**\n- Two space-separated integers representing the indices (0-indexed), sorted ascending.",
    "difficulty": "Easy",
    "constraints": ["2 <= n <= 10^4", "-10^9 <= nums[i] <= 10^9", "-10^9 <= target <= 10^9"],
    "starterCode": {
      "javascript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim().split('\\n');\n\nfunction solve() {\n    const n = parseInt(input[0]);\n    const nums = input[1].trim().split(' ').map(Number);\n    const target = parseInt(input[2]);\n    \n    // Your logic here\n    // console.log(result);\n}\nsolve();",
      "python": "import sys\n\ndef solve():\n    data = sys.stdin.read().split()\n    if not data: return\n    n = int(data[0])\n    nums = [int(x) for x in data[1:n+1]]\n    target = int(data[n+1])\n    \n    # Your code here\n\nif __name__ == '__main__':\n    solve()",
      "cpp": "#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    int n;\n    if (!(cin >> n)) return 0;\n    vector<int> nums(n);\n    for(int i=0; i<n; i++) cin >> nums[i];\n    int target;\n    cin >> target;\n    \n    // Your code here\n    \n    return 0;\n}"
    },
    "testCases": [
      { "input": "4\n2 7 11 15\n9", "output": "0 1", "isPublic": true },
      { "input": "3\n3 2 4\n6", "output": "1 2", "isPublic": true }
    ]
  },
  {
    "title": "Palindrome Number",
    "slug": "palindrome-number",
    "description": "Given an integer `x`, return `true` if `x` is a palindrome, and `false` otherwise.\n\n**Input Format:**\n- Single integer `x`\n\n**Output Format:**\n- string \"true\" or \"false\"",
    "difficulty": "Easy",
    "constraints": ["-2^31 <= x <= 2^31 - 1"],
    "starterCode": {
      "javascript": "const fs = require('fs');\nconst input = fs.readFileSync(0, 'utf-8').trim();\n\nfunction solve() {\n    const x = parseInt(input);\n    // Your code here\n    console.log(\"true\");\n}\nsolve();",
      "python": "import sys\n\ndef solve():\n    x = sys.stdin.read().strip()\n    # Your code here\n    print(\"true\")\n\nif __name__ == '__main__':\n    solve()",
      "cpp": "#include <iostream>\n#include <string>\n#include <algorithm>\nusing namespace std;\n\nint main() {\n    long long x;\n    cin >> x;\n    // Your code here\n    return 0;\n}"
    },
    "testCases": [
      { "input": "121", "output": "true", "isPublic": true },
      { "input": "-121", "output": "false", "isPublic": true },
      { "input": "10", "output": "false", "isPublic": true }
    ]
  },
  {
    "title": "Valid Parentheses",
    "slug": "valid-parentheses",
    "description": "Given a string `s` containing just the characters '(', ')', '{', '}', '[' and ']', determine if the input string is valid.\n\n**Input Format:**\n- A single string `s`\n\n**Output Format:**\n- \"true\" or \"false\"",
    "difficulty": "Medium",
    "constraints": ["1 <= s.length <= 10^4"],
    "starterCode": {
      "javascript": "const fs = require('fs');\nconst s = fs.readFileSync(0, 'utf-8').trim();\n\n// Solve\nconsole.log(\"true\");",
      "python": "import sys\ns = sys.stdin.read().strip()\n# Solve\nprint(\"true\")",
      "cpp": "#include <iostream>\n#include <stack>\n#include <string>\nusing namespace std;\n\nint main() {\n    string s;\n    cin >> s;\n    // Code here\n    cout << \"true\";\n    return 0;\n}"
    },
    "testCases": [
      { "input": "()", "output": "true", "isPublic": true },
      { "input": "()[]{}", "output": "true", "isPublic": true },
      { "input": "(]", "output": "false", "isPublic": true }
    ]
  },
  {
    "title": "Maximum Subarray",
    "slug": "maximum-subarray",
    "description": "Given an integer array `nums`, find the contiguous subarray (containing at least one number) which has the largest sum and return *its sum*.\n\n**Input Format:**\n- First line: `n`\n- Second line: `n` integers\n\n**Output Format:**\n- Single integer (Max Sum)",
    "difficulty": "Medium",
    "constraints": ["1 <= n <= 10^5", "-10^4 <= nums[i] <= 10^4"],
    "starterCode": {
      "javascript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nconst n = parseInt(lines[0]);\nconst nums = lines[1].split(' ').map(Number);\n// Solve",
      "python": "import sys\ndata = sys.stdin.read().split()\nn = int(data[0])\nnums = [int(x) for x in data[1:]]\n# Solve",
      "cpp": "#include <iostream>\n#include <vector>\nusing namespace std;\nint main() {\n    int n; cin >> n;\n    vector<int> nums(n);\n    for(int i=0; i<n; i++) cin >> nums[i];\n    // Solve\n    return 0;\n}"
    },
    "testCases": [
      { "input": "9\n-2 1 -3 4 -1 2 1 -5 4", "output": "6", "isPublic": true },
      { "input": "1\n1", "output": "1", "isPublic": true }
    ]
  },
  {
    "title": "Fizz Buzz",
    "slug": "fizz-buzz",
    "description": "Given an integer `n`, return a string array `answer` where:\n`answer[i] == \"FizzBuzz\"` if `i` is divisible by 3 and 5.\n`answer[i] == \"Fizz\"` if `i` is divisible by 3.\n`answer[i] == \"Buzz\"` if `i` is divisible by 5.\n`answer[i] == i` (as a string) otherwise.\n\n**Input Format:**\n- Single integer `n`\n\n**Output Format:**\n- Space-separated strings",
    "difficulty": "Easy",
    "constraints": ["1 <= n <= 10^4"],
    "starterCode": {
      "javascript": "const fs = require('fs');\nconst n = parseInt(fs.readFileSync(0, 'utf-8').trim());\n// Print result joined by space",
      "python": "import sys\nn = int(sys.stdin.read().strip())\n# Print result",
      "cpp": "#include <iostream>\nusing namespace std;\nint main() {\n    int n; cin >> n;\n    // Solve\n    return 0;\n}"
    },
    "testCases": [
      { "input": "3", "output": "1 2 Fizz", "isPublic": true },
      { "input": "5", "output": "1 2 Fizz 4 Buzz", "isPublic": true }
    ]
  },
  {
    "title": "Reverse String",
    "slug": "reverse-string",
    "description": "Write a function that reverses a string. The input string is given as an array of characters `s`.\n\n**Input Format:**\n- A single string `s`\n\n**Output Format:**\n- The reversed string",
    "difficulty": "Easy",
    "constraints": ["1 <= s.length <= 10^5"],
    "starterCode": {
      "javascript": "const fs = require('fs');\nconst s = fs.readFileSync(0, 'utf-8').trim();\nconsole.log(s.split('').reverse().join(''));",
      "python": "import sys\ns = sys.stdin.read().strip()\nprint(s[::-1])",
      "cpp": "#include <iostream>\n#include <string>\n#include <algorithm>\nusing namespace std;\nint main() {\n    string s; cin >> s;\n    reverse(s.begin(), s.end());\n    cout << s;\n    return 0;\n}"
    },
    "testCases": [
      { "input": "hello", "output": "olleh", "isPublic": true },
      { "input": "Hannah", "output": "hannaH", "isPublic": true }
    ]
  },
  {
    "title": "Climbing Stairs",
    "slug": "climbing-stairs",
    "description": "You are climbing a staircase. It takes `n` steps to reach the top. Each time you can either climb 1 or 2 steps. In how many distinct ways can you climb to the top?\n\n**Input Format:**\n- Single integer `n`\n\n**Output Format:**\n- Single integer (number of ways)",
    "difficulty": "Easy",
    "constraints": ["1 <= n <= 45"],
    "starterCode": {
      "javascript": "const fs = require('fs');\nconst n = parseInt(fs.readFileSync(0, 'utf-8').trim());\n// Solve",
      "python": "import sys\nn = int(sys.stdin.read().strip())\n# Solve",
      "cpp": "#include <iostream>\nusing namespace std;\nint main() {\n    int n; cin >> n;\n    // Solve\n    return 0;\n}"
    },
    "testCases": [
      { "input": "2", "output": "2", "isPublic": true },
      { "input": "3", "output": "3", "isPublic": true }
    ]
  },
  {
    "title": "Single Number",
    "slug": "single-number",
    "description": "Given a non-empty array of integers `nums`, every element appears twice except for one. Find that single one.\n\n**Input Format:**\n- First line: `n`\n- Second line: `n` integers\n\n**Output Format:**\n- Single integer",
    "difficulty": "Easy",
    "constraints": ["1 <= n <= 3 * 10^4"],
    "starterCode": {
      "javascript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nconst nums = lines[1].split(' ').map(Number);\n// Solve",
      "python": "import sys\ndata = sys.stdin.read().split()\nnums = [int(x) for x in data[1:]]\n# Solve",
      "cpp": "#include <iostream>\n#include <vector>\nusing namespace std;\nint main() {\n    int n; cin >> n;\n    int ans = 0;\n    for(int i=0; i<n; i++) { int x; cin >> x; ans ^= x; }\n    cout << ans;\n    return 0;\n}"
    },
    "testCases": [
      { "input": "3\n2 2 1", "output": "1", "isPublic": true },
      { "input": "5\n4 1 2 1 2", "output": "4", "isPublic": true }
    ]
  },
  {
    "title": "Missing Number",
    "slug": "missing-number",
    "description": "Given an array `nums` containing `n` distinct numbers in the range `[0, n]`, return the only number in the range that is missing from the array.\n\n**Input Format:**\n- First line: `n`\n- Second line: `n` integers\n\n**Output Format:**\n- Single integer",
    "difficulty": "Easy",
    "constraints": ["n == nums.length", "1 <= n <= 10^4"],
    "starterCode": {
      "javascript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nconst nums = lines[1].split(' ').map(Number);\n// Solve",
      "python": "import sys\ndata = sys.stdin.read().split()\nnums = [int(x) for x in data[1:]]\n# Solve",
      "cpp": "#include <iostream>\n#include <vector>\nusing namespace std;\nint main() {\n    int n; cin >> n;\n    // Solve\n    return 0;\n}"
    },
    "testCases": [
      { "input": "3\n3 0 1", "output": "2", "isPublic": true },
      { "input": "2\n0 1", "output": "2", "isPublic": true }
    ]
  },
  {
    "title": "Contains Duplicate",
    "slug": "contains-duplicate",
    "description": "Given an integer array `nums`, return `true` if any value appears at least twice in the array, and return `false` if every element is distinct.\n\n**Input Format:**\n- First line: `n`\n- Second line: `n` integers\n\n**Output Format:**\n- \"true\" or \"false\"",
    "difficulty": "Easy",
    "constraints": ["1 <= n <= 10^5"],
    "starterCode": {
      "javascript": "const fs = require('fs');\nconst lines = fs.readFileSync(0, 'utf-8').trim().split('\\n');\nconst nums = lines[1].split(' ').map(Number);\n// Solve",
      "python": "import sys\ndata = sys.stdin.read().split()\nnums = [int(x) for x in data[1:]]\n# Solve",
      "cpp": "#include <iostream>\n#include <vector>\n#include <algorithm>\nusing namespace std;\nint main() {\n    // Solve\n    return 0;\n}"
    },
    "testCases": [
      { "input": "4\n1 2 3 1", "output": "true", "isPublic": true },
      { "input": "4\n1 2 3 4", "output": "false", "isPublic": true }
    ]
  }
]